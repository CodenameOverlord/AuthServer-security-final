server.port=9068
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${AUTH_SERVICE_DB_USERNAME}
spring.datasource.password=${AUTH_SERVICE_DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.springframework.security=trace

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


#Springsec

#spring.security.oauth2.authorizationserver.client.oidc-client.registration.client-id="oidc-client"
#client-secret="{noop}secret"
#client-authentication-methods="client_secret_basic"
#authorization-grant-types="authorization_code","refresh_token"
#redirect-uris="http://127.0.0.1:8080/login/oauth2/code/oidc-client"
#post-logout-redirect-uris="http://127.0.0.1:8080/"
#scopes="openid","profile"
#require-authorization-consent=true

#Add in global variable
#DATABASE_URL=jdbc:mysql://localhost:3306/url_name;AUTH_SERVICE_DB_USERNAME=auth service username;AUTH_SERVICE_DB_PASSWORD=auth service password